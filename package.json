{
  "name": "chrod-website",
  "private": true,
  "scripts": {
    "build-index": "node scripts/build-song-index.mjs",
    "build": "npm run build-index",
    "deploy:pages": "npm run build-index && wrangler pages deploy . --project-name=acordeshub"
  },
  "devDependencies": {
    "wrangler": "^4.81.0"
  }
}
